version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: fisio-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - fisio-network

  backend:
    build: ./server
    container_name: fisio-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGODB_URI: mongodb://mongodb:27017/fisiored
    volumes:
      - ./server:/app
      - /app/node_modules
      - backend_logs:/var/log/backend
    depends_on:
      - mongodb
    networks:
      - fisio-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: fisio-frontend
    restart: always
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - fisio-network

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: fisio-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    networks:
      - fisio-network

  logstash:
    image: docker.elastic.co/logstash/logstash:8.11.0
    container_name: fisio-logstash
    ports:
      - "5001:5000"
    volumes:
      - ./logstash/pipeline:/usr/share/logstash/pipeline
      - backend_logs:/var/log/backend
    depends_on:
      - elasticsearch
    networks:
      - fisio-network

  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    container_name: fisio-kibana
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    networks:
      - fisio-network

  prometheus:
    image: prom/prometheus:latest
    container_name: fisio-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - fisio-network

  grafana:
    image: grafana/grafana:latest
    container_name: fisio-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - fisio-network

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: fisio-cadvisor
    ports:
      - "8080:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    privileged: true
    networks:
      - fisio-network

volumes:
  mongo_data:
  elasticsearch_data:
  prometheus_data:
  grafana_data:
  backend_logs:

networks:
  fisio-network:
    driver: bridge